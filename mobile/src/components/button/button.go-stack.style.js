import {
  StyleSheet,
} from 'react-native';

export default styles = StyleSheet.create({
  button: {
    justifyContent: 'center',
    alignItems: 'center'
  },
  button: {
    height: 42,
    backgroundColor: '#f05a5b',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 2,
  },
  text: {
    color: '#FFF',
    fontWeight: 'bold',
    fontSize: 16,
  }
});