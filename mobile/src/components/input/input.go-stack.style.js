import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
    label: {
        fontWeight: 'bold',
        color: '#444',
        marginBottom: 8,

    },
    input: {
        borderWidth: 1,
        borderColor: '#ddd',
        paddingHorizontal: 20,
        fontSize: 16,
        color: '#444',
        width: '100%',
        height: 44,
        marginBottom: 20,
        borderRadius: 2,
    }
})